/* create an index so that we can easily access these shaders whereever we are in the file system */

import vertexShader from './lightBeam.vert';
import fragmentShader from './lightBeam.frag';

export { vertexShader, fragmentShader };